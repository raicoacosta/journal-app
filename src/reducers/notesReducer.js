import { types } from "../types/types";

const initialState = {
    notes: [],
    active: null
}

export const notesReducer = ( state = { }, action) => {
    
    switch (action.type) {

        default:
            return state;
    }
}
